{
  "name": "roster-burrito-releases",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5175",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4175"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vite": "^6.4.2"
  }
}
